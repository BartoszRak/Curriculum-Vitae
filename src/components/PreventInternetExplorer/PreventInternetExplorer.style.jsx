export default ({ spacing }) => ({
  button: {
    fontWeight: 'normal',
    marginButton: spacing.unit * 0.5,
    marginTop: spacing.unit * 0.5,
  },
  root: {
    padding: spacing.unit * 1,
  },
})
